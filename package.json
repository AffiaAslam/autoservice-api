{
  "name": "autoservice-booking-api",
  "version": "1.0.0",
  "description": "Mock booking API for Autoservice.nu — replace with digitalworkshop.nu API later",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
